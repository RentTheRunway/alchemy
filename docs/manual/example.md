[< back to Manual](../manual.md)

#Alchemy Example, Step by Step

The `alchemy-example` module provides you with an example application that uses the `alchemy-core` module to create, configure and query experiments.